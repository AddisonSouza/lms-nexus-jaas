# Grill Me
- [x] De onde o front obtém o nome? — Claims `name` e `email` no access token (JWT); sem endpoint novo.
- [x] Quais headers mostram o nome? — Ambos: `Header` (AppShell) e `MinimalHeader`.
- [x] Como exibir? — Nome completo truncado (ellipsis + max-width), nome inteiro no `title`.
